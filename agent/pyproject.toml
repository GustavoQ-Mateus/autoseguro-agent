[project]
name = "autoseguro-agent"
version = "1.0.0"
description = "Agente de atendimento e cotacao AutoSeguro (desafio FDE Namastex)"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "pydantic>=2.6",
    "httpx>=0.27",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "pytest>=8.2",
]
